server:
  port: 9002

spring:
  application:
    name: traveler-service
  datasource:
    url: jdbc:mysql://localhost:3312/traveler?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: examen
    password: examen123
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/